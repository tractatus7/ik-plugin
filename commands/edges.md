---
description: Edge-case checklist before writing tests
---
For $ARGUMENTS (or the code we just discussed), list the edge cases worth
testing: empty/zero/null, single element, boundaries, duplicates, invalid input,
scale. One line each: input -> expected output, with a half-line justification.
Do NOT write test code yet — wait for me to pick which ones matter.
